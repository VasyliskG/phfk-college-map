# 🗺️ Віртуальна карта ПГФК

Інтерактивна карта коледжу [Природничо-гуманітарного фахового коледжу](https://www.college.uzhnu.edu.ua/) з функціями навігації по аудиторіях та пошуку.

🌐 **Вебсайт проекту:** https://www.phfk-college-map.website/

---

## 📋 Про проект

Це веб-застосунок, що надає студентам, викладачам та гостям коледжу зручний способ:
- 🏫 Переглядати інтерактивну карту коледжу за поверхами
- 🔍 Шукати аудиторії за номером або призначенням
- 🛣️ Будувати оптимальні маршрути між кімнатами
- 📱 Використовувати на мобільних та десктопних пристроях

---

## 🛠️ Технологічний стек

### Frontend
- **HTML5**, **CSS3**, **JavaScript** (ванільний)
- Адаптивний дизайн для мобільних пристроїв
- Интерактивна карта з рендерингом поверхів

### Backend
- **Node.js** з фреймворком **Express.js**
- REST API для управління даними аудиторій
- Алгоритм маршрутизації (Dijkstra) для знаходження найкоротшого шляху
- CORS включений для крос-доменних запитів

### Deployment
- **Docker** для контейнеризації
- **Docker Compose** для оркестрації сервісів
- **Nginx** для проксування та статичних файлів
- **Cloudflared** для захищеного туннелю

---

## 📁 Структура проекту

```
phfk-college-map/
├── frontend/                 # Фронтенд-частина
│   ├── index.html           # Головна сторінка
│   ├── map.html             # Сторінка з картою
│   ├── about.html           # Інформація про коледж
│   ├── how-to-use.html      # Інструкція користування
│   ├── js/                  # JavaScript файли
│   │   ├── main.js          # Головний скрипт
│   │   ├── api.js           # Клієнтська API
│   │   ├── mapRenderer.js   # Рендеринг карти
│   │   ├── routing.js       # Логіка маршрутизації
│   │   └── search.js        # Функціонал пошуку
│   ├── styles/              # CSS стилі
│   └── images/              # Зображення та іконки
├── backend/                 # Бекенд-частина
│   ├── src/
│   │   └── server.js        # Основний сервер Express
│   ├── data/                # Дані коледжу
│   │   ├── rooms.json       # Перелік аудиторій
│   │   └── graph.json       # Граф для маршрутизації
│   └── package.json         # Залежності Node.js
├── cloudflared/             # Конфіг для Cloudflare Tunnel
├── docker-compose.yml       # Оркестрація Docker контейнерів
├── Dockerfile               # Образ для бекенду
├── Dockerfile.frontend      # Образ для фронтенду
├── nginx.conf               # Конфіг веб-сервера
└── README.md                # Цей файл
```

---

## 🚀 Встановлення та запуск

### Варіант 1: Docker (рекомендується)

**Вимоги:**
- Docker 20.10+
- Docker Compose 1.29+

**Запуск:**

```bash
cd phfk-college-map
docker-compose up -d
```

Додаток буде доступний за адресою: `http://localhost`

### Варіант 2: Локальне розроблення

**Вимоги:**
- Node.js 14.0+ та npm/yarn
- Сучасний браузер

**Встановлення бекенду:**

```bash
cd backend
npm install
npm start       # або npm run dev для розроблення
```

Сервер запуститься на `http://localhost:3000`

**Запуск фронтенду:**

```bash
cd frontend
# Використовуйте будь-який простий веб-сервер
python3 -m http.server 8000
# або
npx http-server
```

Додаток буде доступний за адресою: `http://localhost:8000`

---

## 📡 API Endpoints

### Аудиторії

```http
GET /api/rooms
```
Повертає список всіх аудиторій коледжу.

**Відповідь:**
```json
{
  "rooms": [
    {
      "roomId": "101",
      "name": "Аудиторія 101",
      "floor": 1,
      "type": "classroom",
      "nodeId": "node_101"
    }
  ]
}
```

```http
GET /api/rooms/:id
```
Повертає інформацію про конкретну аудиторію.

### Граф

```http
GET /api/graph
```
Повертає граф будівлі для маршрутизації.

### Пошук

```http
GET /api/search?q=<query>
```
Шукає аудиторії за номером або назвою.

**Приклад:**
```http
GET /api/search?q=101
```

### Маршрутизація

```http
GET /api/route?from=<node1>&to=<node2>
```
Знаходить оптимальний маршрут між двома точками.

**Приклад:**
```http
GET /api/route?from=node_101&to=node_205
```

**Відповідь:**
```json
{
  "path": ["node_101", "node_corridor", "node_205"],
  "distance": 45.5,
  "rooms": [
    { "roomId": "101", "name": "Аудиторія 101", "floor": 1 }
  ]
}
```

### Здоров'я сервера

```http
GET /health
```
Перевіряє статус сервера.

---

## 🎨 Функціональні можливості

### ✨ Реалізовано
- ✅ Інтерактивна карта будівлі з поверхами
- ✅ Пошук аудиторій за номером/назвою
- ✅ Навігація та побудова маршрутів
- ✅ Інформація про контакти коледжу
- ✅ Адаптивна верстка для мобільних
- ✅ Темна та світла тема (в залежності від контексту)
- ✅ API для інтеграції з іншими сервісами

### 🚧 Можливі розширення
- [ ] Збереження улюблених маршрутів
- [ ] Голосова навігація
- [ ] QR-коди для швидкого доступу до помещень
- [ ] Інтеграція з розписом занять
- [ ] Багатомовна підтримка
- [ ] Оффлайн-режим

---

## 🔧 Конфігурація

### Змінні оточення (Backend)

Створіть файл `.env` у папці `backend`:

```env
PORT=3000
NODE_ENV=production
CORS_ORIGIN=*
```

### Nginx

Конфіг знаходиться у файлі `nginx.conf` і налаштований для:
- Проксування API запитів на бекенд
- Обслуговування статичних файлів фронтенду
- Кеширування ресурсів

---

## 🤝 Внесок у проект

Ми вітаємо внески до проекту! Якщо ви знайшли баг або маєте ідею для покращення:

1. Зробіть fork репозиторію
2. Створіть гілку для вашої функції (`git checkout -b feature/AmazingFeature`)
3. Зробіть commit змін (`git commit -m 'Add some AmazingFeature'`)
4. Зробіть push у гілку (`git push origin feature/AmazingFeature`)
5. Відкрийте Pull Request

---

## 📝 Ліцензія

Проект ліцензований під ліцензією, вказаною в файлі [LICENSE](LICENSE).

---

## 📞 Контакти

**Природничо-гуманітарний фаховий коледж:**
- 📞 Телефон: (0322) 61-33-45
- 📍 Адреса: Закарпатська обл., м. Ужгород, вул. Українська, 89
- 📧 Email: collegezhnu@ukr.net
- 🌐 Сайт: https://www.college.uzhnu.edu.ua/

---

## 📚 Додаткова інформація

### Розроблення

Для розроблення з гарячою перезагрузкою використовуйте:

```bash
cd backend
npm run dev
```

### Логування

Логи Docker контейнерів можна переглянути:

```bash
docker-compose logs -f api       # Логи бекенду
docker-compose logs -f frontend  # Логи фронтенду
```

### Відлагодження

Щоб отримати детальні логи сервера, встановіть змінну оточення:

```bash
NODE_ENV=development docker-compose up
```

---

**Останнє оновлення:** Лютий 2026

**Версія:** 1.0.0
